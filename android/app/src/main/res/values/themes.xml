<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- shortEdges — чтобы страница уходила под чёлку, а вырез компенсировался
         на стороне игры через viewport-fit=cover + env(safe-area-inset-*). -->
    <style name="Theme.Ash" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <style name="Theme.Ash.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Ash</item>
    </style>
</resources>
