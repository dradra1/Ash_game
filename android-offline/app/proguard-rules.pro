# Оболочка — один Activity с WebView: рефлексии нет, JSON нет, сети своими
# руками нет. Правил R8 не требуется, файл остаётся точкой расширения.
#
# @JavascriptInterface тоже не используется: связь с игрой односторонняя, через
# evaluateJavascript. Появится интерфейс — сюда придёт -keepclassmembers на его
# методы, иначе R8 срежет их и вызовы из JS молча перестанут работать.
