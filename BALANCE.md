# Заметки по балансу

Рабочий журнал числовых наблюдений. Инструмент — `node tools/playtest.js`
(бот-кайтер, поволновая диагностика). Полный проход по 4 сложностям и составам
1 / 2 / 4 / 8 игроков — этап **M7**; здесь копятся факты, найденные по дороге.

## Найдено на M2

1. **Темп спавна из ТЗ был неигровым.** Формула `(2 + 0.9*wave)` даёт 2.9 врага/с
   уже на первой волне — стартовое оружие физически не успевает, толпа копится и
   топит игрока к 3-й волне. Подобрано `budget_base 0.5`, `budget_per_wave 0.30`
   (волна 1 — 0.8/с, волна 20 — 6.5/с). Требует перепроверки после M4, когда
   появятся все 28 оружий: сейчас у игрока их 1–2, дальше будет 6.

2. **Ранний доход не давал играть в лавку.** При 1 праха за культиста после первой
   волны нельзя купить ничего (тир-1 стоит 13–15). Поднято до 2/3/4 за культиста,
   плевальщика и ком плоти. После этого первая покупка случается уже после 1-й волны.

3. **Поздняя экономика ломается.** На прогоне с бессмертным игроком к 20-й волне
   накапливается ~43 000 праха и 111 предметов при нулевом дефиците. Доход растёт
   с числом убийств (квадратично по волне), а цены — линейно. Нужен либо
   прогрессивный множитель цен, либо потолок предметов, либо сток праха.
   **Не чинилось на M2** — правится вместе с полным контентом на M4/M7.

4. **Финальный босс редко умирает.** Оружие целится в ближайшего врага, а вокруг
   босса всегда толпа мелочи — босс почти не получает урона. Кандидаты: приоритет
   цели по «боссу в радиусе», отдельный слот приоритета, или чистка арены перед
   боссом. Решать на M4 вместе с элитами.

5. **Бот в `playtest.js` — нижняя граница мастерства.** Он перебирает 16 направлений
   и выбирает то, где ближайший враг дальше всего, с премией за число целей в
   радиусе оружия. Важно: если сделать его чисто уклоняющимся, он не убивает никого
   и умирает от нищеты; если чисто агрессивным — умирает от контактного урона.
   Баланс между этими режимами и есть навык жанра, поэтому боту нельзя давать
   уклоняться дальше, чем бьёт его оружие.

## Ориентиры, к которым идём (DoD M7)

- «Послушник» — проходится новичком соло.
- «Мученик» — челлендж даже с полной метапрогрессией.
- Ввосьмером играбельно и не превращается в кашу.

## Найдено на M4

6. **Причина ранних смертей — темп набора оружия, а не структура.** Эксперимент:
   игрок стоит в центре без движения на первых двух волнах.
   | билд | волна | получено ударов | HP в конце |
   |---|---|---|---|
   | стартовый тесак | 2 | 8 | 0/25 |
   | 6 тесаков тира I | 3 | **0** | 25/25 |
   | 6 картечниц тира IV | 3 | 3 | 19/25 |

   С шестью стволами игрок не получает урона вообще: толпа не доходит. С одним —
   гибнет. Значит лечить надо не живучесть, а скорость набора арсенала.

7. **Подкручено:** `player.iframes` 0.35 → **0.5** (главный защитный рычаг жанра:
   в толпе игрок получает удар раз в iframes секунд независимо от её размера,
   0.35 давало 2.9 удара/с и смерть за 3 секунды контакта) и ранний доход
   (культист 2 → 3 праха). Результат бота на «Послушнике»: волна 2.8 → **5.0**.

8. **Любопытная петля:** на «Ратнике» бот заходит дальше (6.6), чем на
   «Послушнике» (5.0). Выше плотность → больше убийств → больше праха → сильнее
   билд. То есть лёгкая сложность беднее и потому по-своему опаснее. Учесть в
   проходе M7: у низких сложностей, возможно, нужен компенсирующий множитель дропа.

## Правки по списку замечаний (2026-07-26)

9. **Найдена и закрыта причина находки №3** («к 20-й волне ~43 000 праха»). Лавка
   меняла `player.ash` напрямую, а `economy.spend()` не вызывался ни из одной точки
   кода — котёл не знал о тратах, и первый же подобранный на следующей волне прах
   пересчитывал баланс из нетронутого котла, возвращая всё потраченное. Проверено
   воспроизведением: `ash 100 → buy(35) → 65 → syncAsh → 100`. Теперь все деньги
   лавки идут через `createWallet` (`sim/economy.js`), `player.ash` — только витрина.
   Эффект на боте в режиме `--god`: было 53 199 праха и 114 предметов к 19-й волне,
   стало 973 и 93 — то есть деньги наконец тратятся.

10. **Кооп был богаче соло втрое.** Спавн умножал число врагов на
    `budget_per_player` (0.65), а дроп ДОПОЛНИТЕЛЬНО на `ash_per_player` (0.55), и
    только потом котёл делился на N: при восьмерых доля выходила 5.55 × 4.85 / 8 ≈
    3.4 дохода соло. `dropMultiplier` теперь считается как `N / budgetScale`, то есть
    компенсация выводится из того, насколько кооп уже увеличил число врагов, а не
    задаётся независимой константой. Ключ `coop.ash_per_player` удалён, вместо него
    одна ручка `coop.ash_share_target` (1.0 = «на брата столько же, сколько соло»).
    Закреплено тестом `tests/js/economy.test.js` для составов 1/2/4/8.

11. **Кривая дохода приведена к ТЗ.** Цель — прах, полученный ЗА ВОЛНУ:
    50 / 80 на 1-й и 2-й, 200 на 7-й, 600 на 13-й, 800 на 20-й.

    | волна | 1 | 2 | 4 | 7 | 10 | 13 | 16 | 19 |
    |---|---|---|---|---|---|---|---|---|
    | цель | 50 | 80 | 128 | 200 | 400 | 600 | 686 | 771 |
    | факт | 50 | 80 | 129 | 199 | 411 | 613 | 625 | 733 |

    Два рычага. Первый — `stats.tithe_scale`: «десятина» прибавлялась ПЛОСКО к
    каждому убийству, и при 40 накопленной десятины враг ценой 3 приносил 43; за
    волну убийств сотни, отсюда и взрыв поздней экономики (3182 при цели 686).
    Стала долей от праха врага. Второй — `economy.wave_ash_mult`, поволновая
    поправка: убийств растёт почти квадратично по волне, а цель почти линейно.

    **Осторожно при перекалибровке**: доход и сила билда завязаны в петлю (больше
    праха → больше предметов → больше убийств → больше праха), поэтому один прогон
    сходится не сразу и колеблется вокруг цели. Значения подобраны вилкой: прогон
    «+15% к цели» и прогон «−15%», затем середина. Мерить: `node tools/playtest.js
    --god`, колонка «доход» (добавлена там же). Калибровка сделана на ОДНОМ сиде в
    режиме бессмертия — для живого игрока доход ниже, это запас в правильную сторону.

12. **Дальность перестала быть налогом на ближний бой.** `range_melee_cooldown_pct`
    0.35 → **0.12**. Каждое очко дальности удлиняло перезарядку на 35% при выгоде
    в 10 px, а стат лежит в пуле улучшений с весом 5 — пара взятий разгоняла пику
    с 1.05 с до 2+ с на удар. Это и было «оружие блокируется перед атаками».

13. **Пачки спавна не работали вовсе.** `want = min(packSize, floor(credit))`, где
    `packSize = max(minPack, floor(credit))` — то есть `want` всегда равнялся
    `floor(credit)`, и `pack_size_base/pack_size_per_wave` не влияли ни на что.
    Теперь спавнер копит до целой пачки. Бюджет волны при этом не меняется:
    враги не выдаются авансом, меняется только группировка.

14. **Ломаемые объекты не появлялись на первой волне.** Их ставит `spawnBreakables()`
    из `startWave()`, а первая волна через `startWave` не проходит вовсе: её состояние
    выписано прямо в литерале `state`, и функция зовётся только на переходах. Одна
    волна из двадцати шла без единого мини-ивента — ровно та, на которой игрок с ними
    знакомится. Теперь `spawnBreakables()` вызывается ещё и при создании забега.
    Заодно `cheatKillAll` перестал считать бочки убийствами: они живут в пуле врагов,
    и чит начислял за них очки, попутно молча съедая все награды волны.

15. **Доход после ломаемых объектов** (`node tools/playtest.js --god`, тот же сид):

    | волна | 1 | 2 | 4 | 7 | 10 | 13 | 16 | 19 |
    |---|---|---|---|---|---|---|---|---|
    | цель | 50 | 80 | 128 | 200 | 400 | 600 | 686 | 771 |
    | было | 50 | 80 | 129 | 199 | 411 | 613 | 625 | 733 |
    | стало | 58 | 77 | 135 | 194 | 413 | 656 | 720 | 867 |

    Ручки не трогали. Сама урна даёт немного: пять точек на волну, вес урны 10 из 36,
    награда 10 праха — порядка 14 за волну, и то в идеале, потому что автоприцел
    бьёт по бочкам только когда в радиусе нет живых врагов (`sim/weapon.js`), а на
    поздних волнах такого затишья почти не бывает. Остальное — та самая петля обратной
    связи из п. 11: чуть больше праха на ранних волнах → раньше предметы → больше
    убийств. Перекалибровывать по одному прогону нельзя, это ловля шума; отклонение
    на 19-й волне (+12%) лежит в вилке ±15%, которой ручки и подбирались.

16. **Кооп-доход измерен на живой симуляции, а не только формулой.** Раньше сверка
    была только арифметическая (`tests/js/economy.test.js`), а «сколько на самом деле
    получает игрок» никто не считал: `tools/playtest.js` гоняет одного бота. Появился
    `tools/coop_income.js` — тот же кайт-бот (общая голова в `tools/bot_move.js`),
    тот же сид, разное число игроков; боты бессмертны, потому что смерть штрафует
    котёл и меняет состав, а мерить надо формулу дохода.

    Прах **на одного игрока** за волну, сид 4242, сложность 1, без закупки:

    | волна | 1 | 2 | 4 | 8 |
    |---|---|---|---|---|
    | 1 | 70 | 73 | 55 | 45 |
    | 4 | 126 | 123 | 115 | 106 |
    | 8 | 172 | 207 | 195 | 200 |
    | 12 | 371 | — | — | 420 |
    | 16 | 377 | — | — | 360 |

    Итог по составам (к соло = 1.00):

    | прогон | 2 | 4 | 8 |
    |---|---|---|---|
    | 8 волн, сид 4242 | 1.06 | 1.02 | 0.96 |
    | 10 волн, сид 777 | 1.06 | 1.07 | 0.94 |
    | 16 волн, сид 4242 (через босса 10-й) | — | — | 0.97 |
    | 8 волн с закупкой в лавке | 0.89 | 0.93 | 0.88 |

    **Роста от числа игроков нет**: отклонение ±7% без закупки — это шум одного
    прогона, и он не монотонен по составу. Восьмером выходит чуть БЕДНЕЕ, и это
    объяснимо: арена больше (`coop.arena_per_player`), боты расходятся, и врагов у
    каждого в радиусе оружия меньше; плюс `hp_per_player` 0.1 добавляет врагам HP,
    то есть убийство стоит дороже. С закупкой разрыв доходит до 12% в ту же сторону —
    кооп беднее, а не богаче. Направление безопасное: перекос в «богаче» ломает
    лавку, перекос в «беднее» на 10% лечится ручкой `coop.ash_share_target`, если
    вообще решим лечить.

### Что осталось непроверенным

- Кооп проверен на связность: `tools/coop_test.py --players 8 --seconds 60
  --latency 80` — 60 fps у всех, расхождение позиций клиент↔хост 0 px, потерянных
  снапшотов нет, исходящий трафик 3.7–5.4 КБ/с на клиента при бюджете 30.
- Кооп-доход мерился ботами, а не людьми (п. 16). Живые игроки держатся кучнее,
  чем бот, который кайтит от ближайшего врага, — значит у них плотность целей на
  голову выше, и реальный кооп-доход, скорее всего, ближе к соло, чем измеренные
  0.94–0.97. Это ещё один запас в правильную сторону, но проверить его можно только
  на людях.
- Кривая живучести врагов (`waves.hp_growth`/`dmg_growth`) под полный хил в начале
  волны НЕ пересобиралась. Полное восстановление снимает главный источник давления
  жанра, и «Мученик» почти наверняка стал легче — требует прохода по 4 сложностям.

## Отдельно: почему забег с проклятиями не запускался (2026-07-27)

Симптом со стороны игрока: выбрал проклятия, нажал «Начать» — и оказался в главном
меню. Без проклятий всё работало.

Причина — затенение импорта. `sim/run.js` импортирует `refreshStats` (пересчёт статов
игрока из `sim/player.js`) и тут же объявляет ВНУТРИ `createRun` локальную функцию с
тем же именем — сводку по числу сущностей для дебаг-оверлея. Объявление функции
поднимается на всё тело `createRun`, поэтому вызов «наложить моды проклятий и
пересчитать статы» уходил не туда, натыкался на ещё не инициализированный `const stats`
и падал в TDZ (`Cannot access 'stats' before initialization`). Ветка выполнялась
ровно при `curseFx.ids.length > 0` — то есть только с выбранными проклятиями.

Почему не поймали тестами: все проверки проклятий были на чистых формулах
(`resolveCurseFx`, `applyCurseToDanger`, цены, длина волны), и ни одна не собирала
настоящий забег с непустым списком. Закрыто двумя тестами в `tests/js/curses.test.js`:
забег создаётся и живёт две секунды с каждым проклятием по отдельности и со всеми
сразу; моды «Стеклянного обета» действительно доезжают до статов игрока.

Второй дефект, вскрытый этим же: **осечка старта была молчаливой**. Мастер прячет свою
панель до того, как забег стартовал, поэтому любая ошибка выглядит как «вернулись в
меню». Теперь и сетевая ошибка, и исключение при сборке забега возвращают меню с
текстом `ui.error.run_start` и пишут причину в консоль. Заодно шаг проклятий получил
заголовок: он был единственным экраном мастера без названия.

## Метапрогрессия: арены и множитель реликвий (2026-07-27)

**Арены нельзя было открыть — но не там, где казалось.** Механизм работал целиком:
цена лежала в `arenas.*.unlock`, сервер знал `kind: "arena"` (`meta.unlock_price`),
профиль хранил `unlocks.arena`, преран-мастер уже рисовал закрытые арены серыми.
Не было ровно одного — **вкладки арен в Реликварии**, то есть места, где нажать
«Открыть». Добавлена в `ui/meta_ui.js`; на карточке видно то, ради чего арену
покупают: размер бестиария и своя пара боссов.

Цены: 400 и 1000 → **300 и 600**.

**Множитель реликвий за сложность стал прогрессией.** `danger[i].reward_mult`
существовал и раньше, но пять чисел стояли в таблице сами по себе. Теперь они
выводятся из одной ручки `meta.relic_formula.danger_step` как step^i:

| сложность | 0 Послушник | 1 Ратник | 2 Крестоносец | 3 Мученик | 4 Анафема |
|---|---|---|---|---|---|
| было | 1.0 | 1.35 | 1.8 | 2.4 | 3.2 |
| стало (шаг 1.2) | 1.0 | 1.2 | 1.44 | 1.73 | 2.07 |

**Внимание: шаг 1.2 НИЖЕ прежней кривой.** Высокие сложности платят меньше, чем до
патча, и разрыв растёт к «Анафеме»:

| забег | сложность 1 | сложность 3 | сложность 4 |
|---|---|---|---|
| провал на 10-й волне | 51 → 46 | 91 → 66 | 122 → 79 |
| провал на 15-й | 72 → 64 | 127 → 92 | 170 → 110 |
| победа, 20 волн, 2 босса | 136 → 121 | 242 → 175 | 323 → 209 |

Это выбор шага, а не побочный эффект: одна ручка вместо пяти чисел на глаз. Если
окажется, что «Анафема» больше не окупает риска, менять надо `danger_step`, а не
таблицу — таблица пересобирается патчем из шага, и правка руками разъедется с
ручкой при первом же прогоне скрипта.

**Сколько это в забегах.** Победа на «Ратнике» даёт 121 реликвию плюс 50 за первое
прохождение — то есть вторая арена стоит примерно **две победы**, третья — **четыре**.
Проигранный забег до 15-й волны на той же сложности приносит 64, так что арены
копятся и без побед, просто медленнее. Закреплено тестами `tests/py/test_meta.py`:
множители обязаны следовать шагу, соседние сложности обязаны отличаться ровно на шаг,
а покупка арены — списывать ровно цену из конфига.

## Кооп: «желейное» движение у подключившихся (2026-07-27)

Симптом: у хоста управление идеальное, у клиента персонаж после отпускания клавиши
ещё ездит вперёд-назад.

**Причина — неверная мера ошибки предсказания.** Клиент считал её как «позиция
хоста минус наша позиция В МОМЕНТ ОТПРАВКИ подтверждённого ввода». Эти две точки не
совпадают даже при идеальной сети: получив ввод, хост крутит его ещё несколько
тиков, пока не приедет следующий. При вводе 30 Гц и симуляции 60 Гц это два тика,
и разница уходила в поправку `corr`, которая подталкивала игрока **вперёд на ходу**
и **назад после остановки**. Замер на детерминированном стенде (хост и клиент в
одном процессе, задержка 80 мс): после отпускания клавиши персонаж уезжал назад на
**13.7 px**, потом обгонял авторитетную позицию и продолжал ползти.

**Что сделано.**

1. Клиент больше не «подтягивается» к присланной точке, а **пересобирает
   предсказание**: ставит авторитетную позицию и заново проигрывает поверх неё весь
   ввод, который хост ещё не подтвердил. История теперь хранит кадры
   (`seq`, `dt`, вектор, скорость), а не позиции.
2. Предсказание идёт **тем же вводом, что уехал по сети**: вектор сэмплируется в
   момент отправки и квантуется в int8 ровно как в протоколе. Живой ввод между
   отправками свежее сетевого, и прогноз по нему расходится с хостом на каждом
   нажатии.
3. Шаг движения на клиенте — построчная копия `sim/player.js` (нормализация →
   сдвиг → препятствия → стены). Раньше клиент не нормализовал вектор и считал
   радиус иначе.
4. `net.input_hz` 30 → **60**: один пакет на тик симуляции. Пока ввод шёл реже
   тика, хост применял один пакет переменное число раз, и остаток в один тик
   (≈1.7 px) дрожал на остановке. При 60 Гц остаток падает до полпикселя, а
   задержка на управление сокращается вдвое (33 → 16 мс). Трафик вверх — 480 Б/с
   на клиента, бюджет 30 КБ/с из ТЗ про снапшоты вниз и не затронут.
5. Остаток гасится **визуально** (`smooth`, 0.1 с) и по времени, а не «долей за
   кадр»: симуляция стоит на авторитетной позиции, сглаживание только на картинке
   и одинаково на 60 и 144 fps.

**Результат** (тот же стенд, `tests/js/prediction.test.js`): назад после отпускания
— меньше 2 px при задержках 16, 80 и 160 мс; через треть секунды размах меньше
пикселя; конечная позиция сходится с хостом. Восемь браузеров с задержкой 80 мс:
60 fps у всех, расхождение клиент↔хост 0 px, потерянных снапшотов нет, 3.1–3.2 КБ/с
на клиента.

**Осталось как есть**: остаточная дрожь в полпикселя. Она из-за того, что хост
применяет подтверждённый ввод переменное число тиков (сетевой джиттер), и убрать её
до нуля можно только очередью ввода с потиковым потреблением на хосте. Это заметно
более сложная машинерия ради движения меньше пикселя — не делаем, но знаем.

## Десятина: доход в начале волны, а не доля от дропа (2026-07-27)

**Симптом.** Стат «Десятина» невозможно было оценить: он прибавлял долю к праху с
каждого убитого (`ash = e.ash * (1 + tithe * 0.04)`), и его вклад растворялся в
дропе вместе со сложностью, номером волны и проклятиями. Игрок видел только, что
прах «вроде бы капает чуть быстрее».

**Что сделано.** Десятина стала отдельной строкой дохода: на старте каждой волны в
котёл падает сумма стата по всем участникам (`run.payTithe`), `stats.tithe_scale`
1.0 — «сколько стата, столько праха». Все источники удвоены: `ch_scavenger` 4 → 8,
предметы 1/2/4/5/8 → 2/4/8/10/16, `cu_iron_tithe` 1 → 2.

Выплата **не** умножается на `dropMultiplier`, в отличие от дропа с убийств. Это не
упущение: доход персональный, а не с врагов, и сумма по игрокам, поделённая котлом
на N, уже возвращает каждому его собственную десятину. Закреплено тестом
`tests/js/tithe.test.js` для составов 1/2/4/8 — если кто-нибудь добавит туда
компенсацию, восьмером станет вчетверо богаче.

**Чем это оплачено — стат стал ранним.** Выплата плоская, а доход волны растёт:

| волна | 1 | 4 | 7 | 10 | 13 | 16 |
|---|---|---|---|---|---|---|
| цель дохода ТЗ | 50 | 128 | 200 | 400 | 600 | 686 |
| десятина 8 (один предмет) | 8 | 8 | 8 | 8 | 8 | 8 |
| десятина 40 (полный набор) | 40 | 40 | 40 | 40 | 40 | 40 |

То есть к 13-й волне полный набор десятины весит около 7% дохода, а один предмет —
чуть больше процента. Это осознанный выбор формулы (заказчик выбрал буквальное
значение стата вместо привязки к `wave_ash_mult`), а не просмотр: десятина —
ранний разгон, а не поздний множитель. Если позже понадобится сделать её значимой
до конца забега, менять нужно одно место — умножить выплату на
`economy.waveMult(wave)` в `payTithe`.

## Проклятия праха: обе дыры «Милости лавки» (2026-07-27)

**Симптом.** `cu_free_market` обещает «прах не падает», но деньги набегали.

**Причины — две, обе на пути смерти в `damageEnemy`, куда формульные тесты не
заглядывали.** Первая: награда `br_ash_urn` звала `dropAsh` напрямую, мимо
`ash_drop_zero` — это и есть «из-за сундуков падают деньги». Вторая: при
`ash_drop_zero` обнулялся только базовый прах, а доля десятины продолжала капать с
каждого убийства.

**Что сделано.**
1. `ash_drop_zero` теперь значит «на пол не падает ничего» — и у врагов, и у урн.
   Событие `breakable_down` при этом уходит как раньше: оно про разлёт осколков, а
   не про награду.
2. Перед открытием лавки при `shop_free` счёт обнуляется целиком
   (`economy.zero()`): весь ассортимент бесплатный, остаток не значит ничего.
   Гейт именно по `shop_free`, а не по `ash_drop_zero` — у «Десятины без праха»
   прах настоящий доход. `spent` чистится вместе с котлом, иначе потративший ушёл
   бы в минус на весь остаток забега.
3. `cu_ashless_tithe` больше не даёт `tithe` (+0.5 к выплате раз в волну не заметил
   бы никто), а начисляет **0.5 праха прямо в котёл за каждое убийство** — новый
   эффект `ash_per_kill`. Он умножается на `dropMultiplier`, в отличие от десятины:
   убийств в коопе больше в `budgetScale` раз.

Проверено на прогоне 25 секунд соло: без проклятия на полу до 7 кучек одновременно,
с проклятием — 0, а `ash_gained` равен ровно `убийства × 0.5`. Закреплено в
`tests/js/curses.test.js`.

### Найдено по дороге, НЕ чинилось

- **Кооп с закупкой богатеет с 9-й волны.** `node tools/coop_income.js --buy --waves 13`
  даёт восьмером 2.01 дохода соло на голову (по волнам разрыв открывается на 9-й).
  Это **не** следствие правок десятины: тот же прогон на коммите до них даёт 1.90.
  Без закупки (`--waves 10`) расхождение остаётся в пределах 4%. Похоже на пропускную
  способность отстрела: восемь вооружённых игроков выбивают спавн быстрее, чем растёт
  бюджет волны, и `dropMultiplier = N/budgetScale` перестаёт компенсировать. Числа в
  прошлой записи («в коопе не богаче соло») были сняты на более коротком горизонте и
  этот режим не покрывали.
- **Блок `effect` у предметов не исполняется вообще.** `it_tithe_ledger` обещает
  +6 праха в конце волны, `it_gilded_ledger` — +5%, `it_shared_purse` — +12 в котёл;
  в `sim/` нет ни одной строки, читающей `item.effect`. Предметы работают только
  статами. Это касается не только десятины — проверять надо все `effect` в конфиге.

## Реликвии: всё получение ×1.6 (2026-07-27)

Одна ручка `meta.relic_formula.gain_mult` вместо правки `per_wave`/`win`/`per_boss`
по отдельности. Так сделано не из аккуратности: разовый бонус за первое прохождение
живёт в другом ключе (`meta.first_clear_bonus`), и правкой слагаемых «всё получение»
оказалось бы не всем. Множитель входит и в `award_relics`, и в `first_clear_bonus`.

Победа на 20-й волне с двумя боссами, соло, без проклятий:

| сложность | было | стало |
|---|---|---|
| Послушник | 101 | **162** |
| Ратник | 121 | **194** |
| Крестоносец | 146 | **233** |
| Мученик | 174 | **279** |
| Анафема | 209 | **335** |

Бонус за первый клир: 50 → **80** за каждое из трёх (персонаж, арена, сложность),
то есть первая победа на новой связке приносит 240 сверх формулы.

Заодно это закрывает вопрос, оставшийся от прошлой правки: переход множителя
сложности на шаг 1.2 опустил «Анафему» с 323 до 209 за победу. С ×1.6 она даёт 335 —
чуть выше, чем было до перехода на шаг, при том что кривая осталась одной ручкой.

Ориентир по ценам: арены 300 и 600, оружие 120/250/400. То есть победа на средней
сложности окупает тир-1 оружие примерно за один забег, арену — за два-три.

**Чит.** `POST /api/admin/relics` под вайтлистом насыпает `meta.cheat_relics` = 1000.
Сумму задаёт сервер, тело запроса не читается вовсе: реликвии начисляет сервер по
своей формуле (ТЗ §3.10), и «чит на 1000» из тела превратился бы в «чит на сколько
попросишь». Кнопка — в панели читов на паузе, рядом с остальными.

## Музыка: одна громкость на все треки и фейд-ин волны (2026-07-27)

**Симптом.** Часть треков заметно громче остальных, вход в волну звучит ударом.

**Причина одна на оба пункта.** Треки взяты у разных авторов с разным мастерингом,
и разброс интегральной громкости составлял **18.9 LU**:

| трек | было | стало |
|---|---|---|
| `mu_wave_c` | −10.9 LUFS | −17.7 |
| `mu_boss` | −16.2 | −17.9 |
| `mu_wave_a` | −18.6 | −18.3 |
| `mu_wave_b` | −22.0 | −17.9 |
| `mu_menu` | −29.8 | −18.0 |
| **разброс** | **18.9 LU** | **0.6 LU** |

Ручка `gain` в конфиге это вылечить не могла: она умеет только приглушить, а
`mu_menu` требовал множителя около 4 при потолке громкости `<audio>` в единицу.
Поэтому уровень выправлен в самих файлах — `tools/normalize_audio.py`, двухпроходный
`loudnorm` до −18 LUFS / −1.5 dBTP, все `gain` в конфиге стали 1.0. Заодно ушёл
клиппинг: истинный пик у `mu_wave_c` стоял на +1.4 dBFS, у `mu_wave_a` на 0.0.

**Фейды.** Длительности переехали из констант в конфиг: `audio.fade_ms` = 800 на
обычную смену темы, `audio.wave_fade_ms` = 2000 на вход в волну. Кривых теперь две
вместо линейной:

- **кроссфейд равной мощности** (`sin`/`cos`) между двумя треками. У линейного
  сведения на середине проваливается суммарная громкость: два некоррелированных
  трека по 0.5 амплитуды дают мощность 0.5, а не 1;
- **квадратичная** при появлении из тишины. Кривая равной мощности стартует рывком —
  на десятой доле фейда уже 16% громкости; квадратичная на той же точке даёт 1%.

Закреплено в `tests/js/audio_fade.test.js`, включая проверку, что `gain` у треков
остался нейтральным: вернуть туда «поправку на глаз» — значит снова развести
громкости, и заметно это будет только ушами.

Проверено и в браузере, подменой конструктора `Audio` (элементы музыки в DOM не
попадают, `new Audio` создаёт открепленный узел). Переход меню → волна, замеры
громкости двух элементов:

| время | `mu_menu` | `mu_wave_a` | сумма мощностей |
|---|---|---|---|
| +200 мс | 0.484 | 0.126 | 0.250 |
| +600 мс | 0.421 | 0.270 | 0.250 |
| +1200 мс | 0.253 | 0.431 | 0.250 |
| +2000 мс | пауза, элемент отпущен | 0.500 | 0.250 |

Сумма мощностей постоянна на всём переходе — то есть провала на середине нет не
только в формуле, но и на живых элементах.

**Замечено, не чинилось.** `mu_wave_c` — моно и всего 24.8 с петли; `mu_wave_a`
имеет LRA 18 LU, то есть внутри себя гуляет сильнее, чем теперь расходятся треки
между собой. Это вопрос подбора музыки, а не громкости.

## Инженерия и доход коопа

**Механика.** Оружие класса `engi` не бьёт в руках: на арене стоят три его копии
(`engineering.copies`) в случайных точках, бьют статами хозяина. Остальные классы
— ближний, дальний, стихийный — работают из рук, как работали. Список
разворачиваемых классов в `engineering.classes`, перекрытия на самом оружии —
`deploy` и `deploy_copies`.

**Боевой баг, из-за которого всё пришлось мерить дважды.** Гейт сначала стоял на
ЗАБЕГЕ, а не на слоте: в установки уезжал каждый ствол. Игрок оставался с пустыми
руками, весь урон разъезжался по случайным точкам карты, и доля дожития врага
менялась кратно. Снятая тогда таблица роста убийств (2.21 / 3.56 / 5.47 / … /
9.29) описывала не игру, а поломку; после починки гейта её пришлось переснять
целиком. Урок простой: измеренная таблица меряет то состояние игры, в котором
снята, и после любой правки боевой геометрии её надо снимать заново.

**Что было не так с самой моделью дохода.** Компенсация дропа считалась как
`N / (1 + budget_per_player*(N-1))`, то есть по ПЛАНОВОМУ росту спавна. Но доход
зависит не от того, сколько врагов выпущено, а от того, сколько убито, а доля
дожития растёт вместе с плотностью огня в комнате. Разрыв невелик, но
односторонний — он и давал коопу лишние проценты на голову.

Замер `node tools/coop_income.js`, 5 сидов × 10 волн, сложность 1:

| состав | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| рост убийств (измерено) | 1.00 | 1.71 | 2.41 | 3.08 | 3.72 | 4.36 | 5.02 | 5.52 |
| плановый рост спавна | 1.00 | 1.65 | 2.30 | 2.95 | 3.60 | 4.25 | 4.90 | 5.55 |

**Что сделано.** В конфиге лежит `coop.kill_scale` — измеренная таблица, и
компенсация делит N на неё. Плотность врагов не тронута (`budget_per_player` те
же 0.65), меняется только прах.

**Как именно меняется прах.** Фактор компенсации разложен на частоту и размер:
`dropChance = min(1, factor)`, `dropAmount = max(1, factor)`, матожидание
`chance × amount = factor`. Если компенсация меньше единицы — прах падает НЕ С
КАЖДОГО врага, но кучка остаётся полноразмерной: кучка в 0.7 праха читается как
обман, а «падает не с каждого» — понятное правило, и заодно вдвое меньше пикапов
в пуле и в сетевом канале. Если больше единицы (текущая калибровка: 1.17…1.46) —
падает со всех, кучки крупнее. В соло жребий не бросается вовсе, иначе поток rng
забега поехал бы, а сервер валидирует результат по сиду.

**Результат.** 5 сидов × 10 волн, доход на голову к соло:

| состав | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| к соло | 1.00 | 1.01 | 0.99 | 0.99 | 0.99 | 0.99 | 0.96 | 0.96 |

Максимальное отклонение **4%** — лучше прежних 7%, на которых инвариант «кооп не
богаче соло» держался до всех этих правок. Тем же прогоном подтвердилась и
таблица: измеренный рост 1.72 / 2.39 / 3.08 / 3.72 / 4.36 / 4.94 / 5.49 против
записанных в конфиг.

**Инструмент.** `tools/coop_income.js` усредняет по нескольким сидам (по
умолчанию три) и печатает колонку «нужный дроп», из которой таблица и берётся.
Разброс между заходами доходит до 20% на двоих — по одному сиду калибровать
нельзя. Заодно в нём чинилось: убийства суммировались по всей длине прогона, а
доход — по общему для составов окну волн, из-за чего состав, добравшийся до
десятой волны, сравнивался с дошедшим до восьмой, и рост убийств выходил завышен
вдвое. Теперь оба числа считаются по одному окну.

**Бот.** `tools/bot_move.js` премировал направление за «цель в радиусе МОЕГО
оружия» — с инженерией это означало водить толпу туда, где по ней никто не
стреляет. Добавлено покрытие (`coverAt`): прямое притяжение к своему полю огня
(`COVER_W`) и обесценивание целей вне покрытия (`UNCOVERED_KILL`) — но последнее
только для того, у кого в руках пусто, иначе бот с обычным тесаком разучился бы
драться.

**Заодно.** Остаток праха в конце фазы сбора теперь досыпается в котёл
(`sweepPickups` в `sim/run.js`), а не лежит до следующей волны: магнит тянет
≈860 px за 1.2 с и дальний край арены не достаёт. Это не подарок — прах уже
начислен за убийства.

## Синергии оружия (2026-07-29)

За 2/4/6 одетых стволов одного класса — кумулятивные бонусы, все числа в
`config.synergies` (патч `tools/patch_config_synergies.py`). Счёт идёт по слотам,
дубликаты до слияния считаются за штуку каждый — слияние двух стволов в один
снижает счёт, и это рычаг: игрок выбирает между тиром и сетом.

| класс | 2 | 4 | 6 |
|---|---|---|---|
| melee | +3 melee_dmg | +8% attack_speed | веер: ×1.4 дальность, ×1.3 дуга |
| ranged | +3 ranged_dmg | +6% damage | +1 снаряд за выстрел |
| elem | +4 elem_dmg | +10% crit | +1 пробитие |
| engi | +4 engineering | +8 engineering | +1 установка на ствол |

Спецы шестого порога статом не выражаются и живут отдельными числами в
`synergies.specials` — их читают `sim/weapon.js` (руки) и `sim/turret.js` (двор).
Установки спецы рук не наследуют: их бонус — лишняя копия при развёртывании.
Пул выдерживает худший случай: 8 игроков × 6 слотов × (3+1) = 192 при
`max_turrets` 200. Бонусы шестых порогов балансно не проверялись — кандидат в
проход M7: +1 снаряд это грубо ×2 DPS дальнего билда, и, возможно, порог 6 у
ranged должен быть слабее остальных.

### Теговые синергии (там же, 2026-07-29)

Сеты теперь и по тегам оружия (`config.synergies.tags`); ствол участвует сразу
во всех своих сетах — класс плюс каждый тег. Патч `tools/patch_config_synergy_tags.py`.

| тег | 2 | 4 | 6 |
|---|---|---|---|
| precise (44) | +5% crit | +10% crit | криты ×1.5 (новый спец crit_boost) |
| heavy (36) | +6 отброс | +8% урон | +12% урон, +50% отброс |
| spread (36) | +5% атк.скорость | +10 дальность | +1 снаряд |
| gun (36) | +8 дальность | +8% атк.скорость | +20 дальность, +10% атк.скорость |
| blade (28) | +4% атк.скорость | +5% уклонение | веер ударов (как melee) |
| warp (28) | +6 дальность | +8 elem_dmg | +1 пробитие (как elem) |
| primitive (16) | +8 HP | +4 броня | +16 HP, +1 рег |
| construct (12) | +4 инженерия | +6 инженерия | +1 установка (стек с engi → +2) |
| blunt (8) | +5 отброс | +6 броня | +50% отброс, +8 броня |
| holy (4) | +1 рег | +8% вампиризм | +2 рег, +12% вампиризм |

Следствия для баланса, проверить на M7:
- **Стеки шестых порогов мультипликативны/аддитивны между сетами.** 6 клинков =
  веер ×1.4 от melee и ещё ×1.4 от blade (≈×2 дальность дуги); 6 construct =
  +2 установки на ствол (пул раздут до 264: 8×6×(3+2)=240).
- **extra_shot теперь действует на любое стреляющее оружие**, не только класс
  ranged — у spread есть стихийные стволы. Полный ranged+spread билд даёт
  +2 снаряда за выстрел.
- holy (4 оружия) и blunt (8) собираются на 6 только через дубликаты до
  слияния — это осознанная цена сета мелкой серии.

## Десять персонажей с особенностями лоадаута (2026-08-02)

Поле `characters[].unique` существовало с M4, но код его не читал ни разу: все
14 персонажей различались только статами, весами левелапа и стартовым оружием.
Появился движок — `static/js/sim/unique.js` — и десять персонажей, у которых
особенность ломает не число, а **правило обращения с оружием**. Патч —
`tools/patch_config_characters10.py`, записи — в `tools/content_world.py`.
Старые 13 особенностей движок намеренно игнорирует: ни одного их поля-рычага в
таблице нет, поведение прежних персонажей не изменилось.

| персонаж | фракция | слоты | рычаг |
|---|---|---|---|
| Обетник | cov | 1 | ствол считается за 6 в своих сетах; покупка заменяет его с возвратом; +120% урона, +30% атк.скорости |
| Барон Хлама | scrap | 10 | лавка только тиры 1–2; −30% урона |
| Калибровщик | forge | 6 | лавка только `gun`; пороги сетов 1/3/5 |
| Носитель Роя | chit | 6 | лавка только `melee`; +4% атк.скорости и +3% урона за каждый одетый ствол |
| Разломный | rift | 3 | каждый ствол считается за два; +55% урона |
| Разноликий | rift | 6 | +5% урона и +3% скорости за каждый РАЗНЫЙ тег |
| Реликварий | tomb | 5 | проданное и слитое остаётся печатью в синергиях (до 8) |
| Курганный кузнец | tomb | 4 | лавка на 2 тира выше волны, оружие ×1.7 цены |
| Разнобой | mirror | 6 | без дубликатов и слияния; любой затронутый сет — сразу 6-й порог |
| Двоедушный | mirror | 6 | покупка даёт бесплатную вторую копию; слияния нет |

Что проверить на M7:

1. **Обетник самый подозрительный.** Один кадило-цеп даёт ему сразу три сета
   на шестом пороге (melee + blunt + holy): в браузере на первой лавке это
   14 брони, +20% вампиризма, +3 регена при 45 HP. Плата — единственный слот и
   ноль роста через слияния, но связка «holy на 6 с одного ствола» обходит
   осознанную цену мелких сетов (holy = 4 оружия, см. раздел о теговых сетах).
   Кандидаты на подкрутку: `set_count` 6 → 4 или снятие +120% урона.
2. **Разнобой и «сет с одного ствола».** Шесть разных стволов покрывают почти все
   теги, и каждый сет работает на шестом пороге — потенциально +2 снаряда,
   веер, криты ×1.5 одновременно. Сдерживает только запрет слияния (потолок
   тира — покупной) и −15% HP.
3. **Барон Хлама против пула установок.** Худший случай двора вырос: 8 игроков ×
   10 слотов × (3+2 копии) = 400 при `engineering.max_turrets` 264. Пул при
   переполнении молча не спавнит лишнее (`sim/turret.js`, деградация по
   CLAUDE.md §4) — это осознанно: поднимать потолок значит пускать 400
   стреляющих установок в кадр. Проверить, не выглядит ли деградация как баг.
4. **Кузнец покупает тир 3 на первой волне.** Наценка ×1.7 и 4 слота — плата
   умозрительная: доход первой волны всё равно не позволяет купить тир 3, зато
   к пятой он собирает то, что остальным откроется к восьмой.

Попутно чинилось:

- **`mergeAfterBuy` обещал слияние, которого не будет.** При полных слотах и
  `merge_count − 1` копиях покупка ложилась ПОВЕРХ единственной копии, число
  копий не росло, слияние срывалось — прах списан, ствол потерян. Видно стало на
  Обетнике (у него слоты полны всегда), но баг был общий. Теперь без свободного
  слота слияние обещается, только когда копий хватает и без покупаемой.
- **`patch_config_content.py` откатывал `content_version`** (условие `!=` вместо
  `<` при своей версии 4) — вся комната зря перекачивала конфиг.
- **У Артифекса в живом конфиге висели +99 HP** — след `tests/py/test_admin.py`,
  который однажды отработал по `data/game_config.json`. Убрано патчем.

## Снаряд гаснет на дальности оружия (2026-08-02)

Оружие било по тому, что заведомо вне его дистанции атаки. Выбор цели тут ни при
чём: он всегда шёл строго в радиусе (проверено прогоном всех персонажей — ни
одного выстрела по цели за радиусом). Дело было в **времени жизни снаряда**: им
правил один `shape.ttl`, никак не связанный с `range`, и реальный долёт
`speed × ttl` у 14 из 18 стреляющих семейств оказался больше паспортной
дальности:

| оружие | дальность | долёт | перелёт |
|---|---|---|---|
| копьё (`w_lance`) | 420 | 840 | ×2.00 |
| длинный ствол | 460 | 836 | ×1.82 |
| карабин | 360 | 620 | ×1.72 |
| кузнечный дрон | 300 | 500 | ×1.67 |
| гвоздомёт | 280 | 462 | ×1.65 |
| … ещё девять семейств | | | ×1.35–1.63 |
| дробовик, картечь | 200 / 230 | 209 / 240 | ×1.05 |
| пшикалка, огнемёт | 190 / 170 | 182 / 143 | ×0.96 / ×0.84 |

Игрок видел это так: цель взята вплотную к границе радиуса, снаряд летит дальше
и по дороге рвёт врагов и ломаемые объекты вдвое дальше, чем оружие «умеет».
Особенно заметно на копье и длинном стволе — там перелёт равен всей дальности.

Теперь `sim/weapon.js` на выстреле переводит запас хода во время:
`ttl = min(shape.ttl, range / speed)`, где `range` — **эффективная** дальность
(стат «дальность», синергии, множитель установок), та же, по которой выбиралась
цель. Полёт прямой и с постоянной скоростью, поэтому пересчёт делается один раз
на выстреле, а горячий цикл снарядов не платит ничего (bench: 0.033 мс/тик,
без изменений). `min`, а не присваивание: у огнемёта и пшикалки ttl короче
своего же радиуса намеренно — струя не должна доставать до края.

Цена в бою почти нулевая: 24 персонажа × 3 сида, волна 6 — 327 → 320 убийств
(−2%), полученный урон 2069 → 2064. Орда всё равно идёт на игрока, и перелёт
работал только по отставшим. Тир-листу это ничего не двигает.

Осталось расхождением, но осознанным:

- **Огнемёт и пшикалка целятся дальше, чем достают** (170 против 143). Тут
  ошибка в другую сторону — выстрел в пустоту по цели у края радиуса. Лечится
  правкой контента (`range` вниз или `ttl` вверх), а не кодом; вынесено на M7
  вместе с остальной сверкой стволов.
- **Снаряды врагов не ограничены**: `atk.range` 280–460 при долёте 384–672
  (×1.3–1.6). Враг стреляет с упреждением по бегущему игроку, и обрезать его
  выстрел ровно по радиусу — отдельное решение по сложности, а не тот же баг.
