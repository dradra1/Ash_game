# Заметки по балансу

Рабочий журнал числовых наблюдений. Инструмент — `node tools/playtest.js`
(бот-кайтер, поволновая диагностика). Полный проход по 4 сложностям и составам
1 / 2 / 4 / 8 игроков — этап **M7**; здесь копятся факты, найденные по дороге.

## Найдено на M2

1. **Темп спавна из ТЗ был неигровым.** Формула `(2 + 0.9*wave)` даёт 2.9 врага/с
   уже на первой волне — стартовое оружие физически не успевает, толпа копится и
   топит игрока к 3-й волне. Подобрано `budget_base 0.5`, `budget_per_wave 0.30`
   (волна 1 — 0.8/с, волна 20 — 6.5/с). Требует перепроверки после M4, когда
   появятся все 28 оружий: сейчас у игрока их 1–2, дальше будет 6.

2. **Ранний доход не давал играть в лавку.** При 1 праха за культиста после первой
   волны нельзя купить ничего (тир-1 стоит 13–15). Поднято до 2/3/4 за культиста,
   плевальщика и ком плоти. После этого первая покупка случается уже после 1-й волны.

3. **Поздняя экономика ломается.** На прогоне с бессмертным игроком к 20-й волне
   накапливается ~43 000 праха и 111 предметов при нулевом дефиците. Доход растёт
   с числом убийств (квадратично по волне), а цены — линейно. Нужен либо
   прогрессивный множитель цен, либо потолок предметов, либо сток праха.
   **Не чинилось на M2** — правится вместе с полным контентом на M4/M7.

4. **Финальный босс редко умирает.** Оружие целится в ближайшего врага, а вокруг
   босса всегда толпа мелочи — босс почти не получает урона. Кандидаты: приоритет
   цели по «боссу в радиусе», отдельный слот приоритета, или чистка арены перед
   боссом. Решать на M4 вместе с элитами.

5. **Бот в `playtest.js` — нижняя граница мастерства.** Он перебирает 16 направлений
   и выбирает то, где ближайший враг дальше всего, с премией за число целей в
   радиусе оружия. Важно: если сделать его чисто уклоняющимся, он не убивает никого
   и умирает от нищеты; если чисто агрессивным — умирает от контактного урона.
   Баланс между этими режимами и есть навык жанра, поэтому боту нельзя давать
   уклоняться дальше, чем бьёт его оружие.

## Ориентиры, к которым идём (DoD M7)

- «Послушник» — проходится новичком соло.
- «Мученик» — челлендж даже с полной метапрогрессией.
- Ввосьмером играбельно и не превращается в кашу.

## Найдено на M4

6. **Причина ранних смертей — темп набора оружия, а не структура.** Эксперимент:
   игрок стоит в центре без движения на первых двух волнах.
   | билд | волна | получено ударов | HP в конце |
   |---|---|---|---|
   | стартовый тесак | 2 | 8 | 0/25 |
   | 6 тесаков тира I | 3 | **0** | 25/25 |
   | 6 картечниц тира IV | 3 | 3 | 19/25 |

   С шестью стволами игрок не получает урона вообще: толпа не доходит. С одним —
   гибнет. Значит лечить надо не живучесть, а скорость набора арсенала.

7. **Подкручено:** `player.iframes` 0.35 → **0.5** (главный защитный рычаг жанра:
   в толпе игрок получает удар раз в iframes секунд независимо от её размера,
   0.35 давало 2.9 удара/с и смерть за 3 секунды контакта) и ранний доход
   (культист 2 → 3 праха). Результат бота на «Послушнике»: волна 2.8 → **5.0**.

8. **Любопытная петля:** на «Ратнике» бот заходит дальше (6.6), чем на
   «Послушнике» (5.0). Выше плотность → больше убийств → больше праха → сильнее
   билд. То есть лёгкая сложность беднее и потому по-своему опаснее. Учесть в
   проходе M7: у низких сложностей, возможно, нужен компенсирующий множитель дропа.

## Правки по списку замечаний (2026-07-26)

9. **Найдена и закрыта причина находки №3** («к 20-й волне ~43 000 праха»). Лавка
   меняла `player.ash` напрямую, а `economy.spend()` не вызывался ни из одной точки
   кода — котёл не знал о тратах, и первый же подобранный на следующей волне прах
   пересчитывал баланс из нетронутого котла, возвращая всё потраченное. Проверено
   воспроизведением: `ash 100 → buy(35) → 65 → syncAsh → 100`. Теперь все деньги
   лавки идут через `createWallet` (`sim/economy.js`), `player.ash` — только витрина.
   Эффект на боте в режиме `--god`: было 53 199 праха и 114 предметов к 19-й волне,
   стало 973 и 93 — то есть деньги наконец тратятся.

10. **Кооп был богаче соло втрое.** Спавн умножал число врагов на
    `budget_per_player` (0.65), а дроп ДОПОЛНИТЕЛЬНО на `ash_per_player` (0.55), и
    только потом котёл делился на N: при восьмерых доля выходила 5.55 × 4.85 / 8 ≈
    3.4 дохода соло. `dropMultiplier` теперь считается как `N / budgetScale`, то есть
    компенсация выводится из того, насколько кооп уже увеличил число врагов, а не
    задаётся независимой константой. Ключ `coop.ash_per_player` удалён, вместо него
    одна ручка `coop.ash_share_target` (1.0 = «на брата столько же, сколько соло»).
    Закреплено тестом `tests/js/economy.test.js` для составов 1/2/4/8.

11. **Кривая дохода приведена к ТЗ.** Цель — прах, полученный ЗА ВОЛНУ:
    50 / 80 на 1-й и 2-й, 200 на 7-й, 600 на 13-й, 800 на 20-й.

    | волна | 1 | 2 | 4 | 7 | 10 | 13 | 16 | 19 |
    |---|---|---|---|---|---|---|---|---|
    | цель | 50 | 80 | 128 | 200 | 400 | 600 | 686 | 771 |
    | факт | 50 | 80 | 129 | 199 | 411 | 613 | 625 | 733 |

    Два рычага. Первый — `stats.tithe_scale`: «десятина» прибавлялась ПЛОСКО к
    каждому убийству, и при 40 накопленной десятины враг ценой 3 приносил 43; за
    волну убийств сотни, отсюда и взрыв поздней экономики (3182 при цели 686).
    Стала долей от праха врага. Второй — `economy.wave_ash_mult`, поволновая
    поправка: убийств растёт почти квадратично по волне, а цель почти линейно.

    **Осторожно при перекалибровке**: доход и сила билда завязаны в петлю (больше
    праха → больше предметов → больше убийств → больше праха), поэтому один прогон
    сходится не сразу и колеблется вокруг цели. Значения подобраны вилкой: прогон
    «+15% к цели» и прогон «−15%», затем середина. Мерить: `node tools/playtest.js
    --god`, колонка «доход» (добавлена там же). Калибровка сделана на ОДНОМ сиде в
    режиме бессмертия — для живого игрока доход ниже, это запас в правильную сторону.

12. **Дальность перестала быть налогом на ближний бой.** `range_melee_cooldown_pct`
    0.35 → **0.12**. Каждое очко дальности удлиняло перезарядку на 35% при выгоде
    в 10 px, а стат лежит в пуле улучшений с весом 5 — пара взятий разгоняла пику
    с 1.05 с до 2+ с на удар. Это и было «оружие блокируется перед атаками».

13. **Пачки спавна не работали вовсе.** `want = min(packSize, floor(credit))`, где
    `packSize = max(minPack, floor(credit))` — то есть `want` всегда равнялся
    `floor(credit)`, и `pack_size_base/pack_size_per_wave` не влияли ни на что.
    Теперь спавнер копит до целой пачки. Бюджет волны при этом не меняется:
    враги не выдаются авансом, меняется только группировка.

14. **Ломаемые объекты не появлялись на первой волне.** Их ставит `spawnBreakables()`
    из `startWave()`, а первая волна через `startWave` не проходит вовсе: её состояние
    выписано прямо в литерале `state`, и функция зовётся только на переходах. Одна
    волна из двадцати шла без единого мини-ивента — ровно та, на которой игрок с ними
    знакомится. Теперь `spawnBreakables()` вызывается ещё и при создании забега.
    Заодно `cheatKillAll` перестал считать бочки убийствами: они живут в пуле врагов,
    и чит начислял за них очки, попутно молча съедая все награды волны.

15. **Доход после ломаемых объектов** (`node tools/playtest.js --god`, тот же сид):

    | волна | 1 | 2 | 4 | 7 | 10 | 13 | 16 | 19 |
    |---|---|---|---|---|---|---|---|---|
    | цель | 50 | 80 | 128 | 200 | 400 | 600 | 686 | 771 |
    | было | 50 | 80 | 129 | 199 | 411 | 613 | 625 | 733 |
    | стало | 58 | 77 | 135 | 194 | 413 | 656 | 720 | 867 |

    Ручки не трогали. Сама урна даёт немного: пять точек на волну, вес урны 10 из 36,
    награда 10 праха — порядка 14 за волну, и то в идеале, потому что автоприцел
    бьёт по бочкам только когда в радиусе нет живых врагов (`sim/weapon.js`), а на
    поздних волнах такого затишья почти не бывает. Остальное — та самая петля обратной
    связи из п. 11: чуть больше праха на ранних волнах → раньше предметы → больше
    убийств. Перекалибровывать по одному прогону нельзя, это ловля шума; отклонение
    на 19-й волне (+12%) лежит в вилке ±15%, которой ручки и подбирались.

16. **Кооп-доход измерен на живой симуляции, а не только формулой.** Раньше сверка
    была только арифметическая (`tests/js/economy.test.js`), а «сколько на самом деле
    получает игрок» никто не считал: `tools/playtest.js` гоняет одного бота. Появился
    `tools/coop_income.js` — тот же кайт-бот (общая голова в `tools/bot_move.js`),
    тот же сид, разное число игроков; боты бессмертны, потому что смерть штрафует
    котёл и меняет состав, а мерить надо формулу дохода.

    Прах **на одного игрока** за волну, сид 4242, сложность 1, без закупки:

    | волна | 1 | 2 | 4 | 8 |
    |---|---|---|---|---|
    | 1 | 70 | 73 | 55 | 45 |
    | 4 | 126 | 123 | 115 | 106 |
    | 8 | 172 | 207 | 195 | 200 |
    | 12 | 371 | — | — | 420 |
    | 16 | 377 | — | — | 360 |

    Итог по составам (к соло = 1.00):

    | прогон | 2 | 4 | 8 |
    |---|---|---|---|
    | 8 волн, сид 4242 | 1.06 | 1.02 | 0.96 |
    | 10 волн, сид 777 | 1.06 | 1.07 | 0.94 |
    | 16 волн, сид 4242 (через босса 10-й) | — | — | 0.97 |
    | 8 волн с закупкой в лавке | 0.89 | 0.93 | 0.88 |

    **Роста от числа игроков нет**: отклонение ±7% без закупки — это шум одного
    прогона, и он не монотонен по составу. Восьмером выходит чуть БЕДНЕЕ, и это
    объяснимо: арена больше (`coop.arena_per_player`), боты расходятся, и врагов у
    каждого в радиусе оружия меньше; плюс `hp_per_player` 0.1 добавляет врагам HP,
    то есть убийство стоит дороже. С закупкой разрыв доходит до 12% в ту же сторону —
    кооп беднее, а не богаче. Направление безопасное: перекос в «богаче» ломает
    лавку, перекос в «беднее» на 10% лечится ручкой `coop.ash_share_target`, если
    вообще решим лечить.

### Что осталось непроверенным

- Кооп проверен на связность: `tools/coop_test.py --players 8 --seconds 60
  --latency 80` — 60 fps у всех, расхождение позиций клиент↔хост 0 px, потерянных
  снапшотов нет, исходящий трафик 3.7–5.4 КБ/с на клиента при бюджете 30.
- Кооп-доход мерился ботами, а не людьми (п. 16). Живые игроки держатся кучнее,
  чем бот, который кайтит от ближайшего врага, — значит у них плотность целей на
  голову выше, и реальный кооп-доход, скорее всего, ближе к соло, чем измеренные
  0.94–0.97. Это ещё один запас в правильную сторону, но проверить его можно только
  на людях.
- Кривая живучести врагов (`waves.hp_growth`/`dmg_growth`) под полный хил в начале
  волны НЕ пересобиралась. Полное восстановление снимает главный источник давления
  жанра, и «Мученик» почти наверняка стал легче — требует прохода по 4 сложностям.

## Отдельно: почему забег с проклятиями не запускался (2026-07-27)

Симптом со стороны игрока: выбрал проклятия, нажал «Начать» — и оказался в главном
меню. Без проклятий всё работало.

Причина — затенение импорта. `sim/run.js` импортирует `refreshStats` (пересчёт статов
игрока из `sim/player.js`) и тут же объявляет ВНУТРИ `createRun` локальную функцию с
тем же именем — сводку по числу сущностей для дебаг-оверлея. Объявление функции
поднимается на всё тело `createRun`, поэтому вызов «наложить моды проклятий и
пересчитать статы» уходил не туда, натыкался на ещё не инициализированный `const stats`
и падал в TDZ (`Cannot access 'stats' before initialization`). Ветка выполнялась
ровно при `curseFx.ids.length > 0` — то есть только с выбранными проклятиями.

Почему не поймали тестами: все проверки проклятий были на чистых формулах
(`resolveCurseFx`, `applyCurseToDanger`, цены, длина волны), и ни одна не собирала
настоящий забег с непустым списком. Закрыто двумя тестами в `tests/js/curses.test.js`:
забег создаётся и живёт две секунды с каждым проклятием по отдельности и со всеми
сразу; моды «Стеклянного обета» действительно доезжают до статов игрока.

Второй дефект, вскрытый этим же: **осечка старта была молчаливой**. Мастер прячет свою
панель до того, как забег стартовал, поэтому любая ошибка выглядит как «вернулись в
меню». Теперь и сетевая ошибка, и исключение при сборке забега возвращают меню с
текстом `ui.error.run_start` и пишут причину в консоль. Заодно шаг проклятий получил
заголовок: он был единственным экраном мастера без названия.

## Метапрогрессия: арены и множитель реликвий (2026-07-27)

**Арены нельзя было открыть — но не там, где казалось.** Механизм работал целиком:
цена лежала в `arenas.*.unlock`, сервер знал `kind: "arena"` (`meta.unlock_price`),
профиль хранил `unlocks.arena`, преран-мастер уже рисовал закрытые арены серыми.
Не было ровно одного — **вкладки арен в Реликварии**, то есть места, где нажать
«Открыть». Добавлена в `ui/meta_ui.js`; на карточке видно то, ради чего арену
покупают: размер бестиария и своя пара боссов.

Цены: 400 и 1000 → **300 и 600**.

**Множитель реликвий за сложность стал прогрессией.** `danger[i].reward_mult`
существовал и раньше, но пять чисел стояли в таблице сами по себе. Теперь они
выводятся из одной ручки `meta.relic_formula.danger_step` как step^i:

| сложность | 0 Послушник | 1 Ратник | 2 Крестоносец | 3 Мученик | 4 Анафема |
|---|---|---|---|---|---|
| было | 1.0 | 1.35 | 1.8 | 2.4 | 3.2 |
| стало (шаг 1.2) | 1.0 | 1.2 | 1.44 | 1.73 | 2.07 |

**Внимание: шаг 1.2 НИЖЕ прежней кривой.** Высокие сложности платят меньше, чем до
патча, и разрыв растёт к «Анафеме»:

| забег | сложность 1 | сложность 3 | сложность 4 |
|---|---|---|---|
| провал на 10-й волне | 51 → 46 | 91 → 66 | 122 → 79 |
| провал на 15-й | 72 → 64 | 127 → 92 | 170 → 110 |
| победа, 20 волн, 2 босса | 136 → 121 | 242 → 175 | 323 → 209 |

Это выбор шага, а не побочный эффект: одна ручка вместо пяти чисел на глаз. Если
окажется, что «Анафема» больше не окупает риска, менять надо `danger_step`, а не
таблицу — таблица пересобирается патчем из шага, и правка руками разъедется с
ручкой при первом же прогоне скрипта.

**Сколько это в забегах.** Победа на «Ратнике» даёт 121 реликвию плюс 50 за первое
прохождение — то есть вторая арена стоит примерно **две победы**, третья — **четыре**.
Проигранный забег до 15-й волны на той же сложности приносит 64, так что арены
копятся и без побед, просто медленнее. Закреплено тестами `tests/py/test_meta.py`:
множители обязаны следовать шагу, соседние сложности обязаны отличаться ровно на шаг,
а покупка арены — списывать ровно цену из конфига.

## Кооп: «желейное» движение у подключившихся (2026-07-27)

Симптом: у хоста управление идеальное, у клиента персонаж после отпускания клавиши
ещё ездит вперёд-назад.

**Причина — неверная мера ошибки предсказания.** Клиент считал её как «позиция
хоста минус наша позиция В МОМЕНТ ОТПРАВКИ подтверждённого ввода». Эти две точки не
совпадают даже при идеальной сети: получив ввод, хост крутит его ещё несколько
тиков, пока не приедет следующий. При вводе 30 Гц и симуляции 60 Гц это два тика,
и разница уходила в поправку `corr`, которая подталкивала игрока **вперёд на ходу**
и **назад после остановки**. Замер на детерминированном стенде (хост и клиент в
одном процессе, задержка 80 мс): после отпускания клавиши персонаж уезжал назад на
**13.7 px**, потом обгонял авторитетную позицию и продолжал ползти.

**Что сделано.**

1. Клиент больше не «подтягивается» к присланной точке, а **пересобирает
   предсказание**: ставит авторитетную позицию и заново проигрывает поверх неё весь
   ввод, который хост ещё не подтвердил. История теперь хранит кадры
   (`seq`, `dt`, вектор, скорость), а не позиции.
2. Предсказание идёт **тем же вводом, что уехал по сети**: вектор сэмплируется в
   момент отправки и квантуется в int8 ровно как в протоколе. Живой ввод между
   отправками свежее сетевого, и прогноз по нему расходится с хостом на каждом
   нажатии.
3. Шаг движения на клиенте — построчная копия `sim/player.js` (нормализация →
   сдвиг → препятствия → стены). Раньше клиент не нормализовал вектор и считал
   радиус иначе.
4. `net.input_hz` 30 → **60**: один пакет на тик симуляции. Пока ввод шёл реже
   тика, хост применял один пакет переменное число раз, и остаток в один тик
   (≈1.7 px) дрожал на остановке. При 60 Гц остаток падает до полпикселя, а
   задержка на управление сокращается вдвое (33 → 16 мс). Трафик вверх — 480 Б/с
   на клиента, бюджет 30 КБ/с из ТЗ про снапшоты вниз и не затронут.
5. Остаток гасится **визуально** (`smooth`, 0.1 с) и по времени, а не «долей за
   кадр»: симуляция стоит на авторитетной позиции, сглаживание только на картинке
   и одинаково на 60 и 144 fps.

**Результат** (тот же стенд, `tests/js/prediction.test.js`): назад после отпускания
— меньше 2 px при задержках 16, 80 и 160 мс; через треть секунды размах меньше
пикселя; конечная позиция сходится с хостом. Восемь браузеров с задержкой 80 мс:
60 fps у всех, расхождение клиент↔хост 0 px, потерянных снапшотов нет, 3.1–3.2 КБ/с
на клиента.

**Осталось как есть**: остаточная дрожь в полпикселя. Она из-за того, что хост
применяет подтверждённый ввод переменное число тиков (сетевой джиттер), и убрать её
до нуля можно только очередью ввода с потиковым потреблением на хосте. Это заметно
более сложная машинерия ради движения меньше пикселя — не делаем, но знаем.
